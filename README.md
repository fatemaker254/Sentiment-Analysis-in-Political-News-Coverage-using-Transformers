# 🗳️ Political News Sentiment Analysis using Pre-Trained Transformers

This project performs sentiment analysis on political news content using a pre-trained Transformer model (`distilbert-base-uncased-finetuned-sst-2-english`).  
It analyzes sentiment categories: **Positive, Neutral, Negative**, and visualizes trends, confidence levels, and party-wise sentiment.

## 🛠️ Requirements

Ensure **Python 3.8+** is installed.  
Download here if needed: https://www.python.org/downloads/windows/

### Install Dependencies
Open **Command Prompt** inside your project folder and run:

1. `python -m venv venv` creating a virtual environment
2. `.\venv\Scripts\activate` activating the environment
3. `pip install -r requirements.txt`
4. Run the file by executing `python main.py`
